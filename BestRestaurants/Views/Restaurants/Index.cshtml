@using BestRestaurants.Models;

@foreach(Restaurant restaurant in @Model)
{
  <p>@Html.ActionLink(restaurant.Name,"Details",new {id = restaurant.RestaurantId}) : @Html.ActionLink(restaurant.CuisineRef.CuisineType,"Details","Cuisines",new {id = restaurant.CuisineRefId}) </p>
}

