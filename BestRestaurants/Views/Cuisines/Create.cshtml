@model BestRestaurants.Models.Cuisine;

<h4>Add a new cuisine</h4>

@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.CuisineType);
  @Html.TextBoxFor(model => model.CuisineType);
  <input type="submit" value="Add cuisine type">
}
