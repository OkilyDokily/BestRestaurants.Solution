@{
  Layout = "_Layout";
}

@using BestRestaurants.Models;

@foreach (Cuisine cuisine in @Model)
{
  <p>@Html.ActionLink(cuisine.CuisineType,"Details",new {id = cuisine.CuisineId})</p>
}
<hr style="width:50%;text-align:left;margin-left:0">
<p>@Html.ActionLink("Make a new cuisine type","Create")</p>